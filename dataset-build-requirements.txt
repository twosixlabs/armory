# Additional requirements needed to build all datasets

# The below are necessary for armory datasets, which
# utilize tfds.  This requires old version of
# pyarrow (0.17.1) which requires pip 22.0.3

apache-beam==2.22.0
# apache-beam==2.41.0

pydub==0.24.1
# pydub==0.25.1
